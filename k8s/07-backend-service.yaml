apiVersion: v1
kind: Service
metadata:
  name: equipment-service
  namespace: equipment-system
spec:
  selector:
    app: equipment-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

