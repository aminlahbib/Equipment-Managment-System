# Database Configuration
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=swtp
MYSQL_USER=appuser
MYSQL_PASSWORD=apppassword

# Spring Boot Configuration
SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/swtp
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=rootpassword

# Frontend Configuration
REACT_APP_API_BASE_URL=/api/benutzer

# JWT Configuration
# Generate a strong secret key (minimum 256 bits / 32 bytes)
# Example: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-strong-random-secret-key-min-256-bits
JWT_EXPIRATION=86400000
